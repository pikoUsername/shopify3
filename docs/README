Очень важная часть по коду.

При импорте из app.models.domain не указывать конкретный файл

При добавлении модели в domain НЕОБХОДИМО его импортировать в __init__.py
затем вызвать метод модели (YOUR_MODEL_NAME).update_forward_refs(**locals())!!
